// Write a function that always returns 5. Just bear in mind that you can't use any of the following characters: 0123456789*+-/

const unusualFive = (n) => {
  return 'easy!'.length
}

unusualFive(5)

// REFACTOR

const unusualFive = n => 'easy!'.length