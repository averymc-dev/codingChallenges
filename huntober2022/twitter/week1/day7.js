/* Today you'll need to create a function that swaps each letter of the alphabet for its opposite. A letter's opposite is one that, if the alphabet were flipped Z-A, would be the same number of letters in, and which would also have the opposite case. */

function swap(str){
  console.log(str)
  let alpha = ['ABCDEFGHIJKLMNOPQRSTUVWXYZ'].toString().split('')
  str.split('')
  // loop through str to check letters
  for(let i=0; i<str.length; i++){
  
    // change letter to other one
    if(str[i] == str[i].toLowerCase()){
      console.log(str[i])
      
    }
    // change casing to other one
    
  }
  // join string
}

swap('vCZNKOV: 0 MFNYVI LI KFMXGFZGRLM XSZMTVH')